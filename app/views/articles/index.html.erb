<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
        ID: <%= article.id %>, Title: <%= link_to article.title, article %>, Body: <%= article.body %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
